# lob-verify-address-api
Verify address using lob address verification rest api

Add the following key to your application configuation file to consume the api
`<add key="LibApiKey" value="<INSERT_YOUR_API_KEY"/>`
